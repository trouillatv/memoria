// Aucune fiche ouverte : la zone parallèle ne rend rien.
// `default.tsx` est ce que Next affiche dans une zone sans segment correspondant
// — au premier chargement d'un onglet comme après un rechargement.
export default function NoFiche() {
  return null
}
