// Layout autonome pour /h/[token] — pas de header dashboard.
// Vincent 2026-05-22.

export default function PublicHandoverLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
