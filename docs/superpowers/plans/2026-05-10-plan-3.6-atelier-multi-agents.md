# Plan 3.6 — Atelier IA multi-agents métier (analyses + challenge)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Étendre l'Atelier IA single-agent (Plan 3.5) en espace **multi-agents métier** où :
1. Chaque agent peut produire sa propre **analyse initiale** de l'AO (à la demande, persistée).
2. L'utilisateur peut sélectionner jusqu'à **3 agents simultanément** dans la composer.
3. Les agents répondent **en parallèle** (Promise.all).
4. Bouton **« Lancer un challenge »** : chaque agent voit et réagit aux réponses des autres.
5. **Max 2 rounds de challenge** par turn (coûts maîtrisés).

**Anti-objectifs explicites (rappel utilisateur) :**
- ❌ Pas de LangGraph
- ❌ Pas d'orchestration autonome
- ❌ Pas de RAG avancé
- ❌ Pas de mémoire long terme cross-tender
- ❌ Pas de streaming token-par-token
- ❌ Pas plus de 3 agents simultanés
- ❌ Pas plus de 2 rounds de challenge

**Architecture :**
- 1 nouvelle migration `015_agent_analyses.sql` (table `tender_agent_analyses`)
- 7 prompts d'analyse initiale par agent (`services/ai/prompts/initial-analysis/<agent>.v1.ts`)
- Server Actions :
  - `runAgentInitialAnalysisAction(tender_id, agent_name)` — single agent
  - `sendChatMessageAction` revisé pour accepter `agent_names: string[]` (1-3) avec parallel fanout
  - `runChallengeRoundAction(tender_id, anchor_message_id, round)` — déclenche 1 round de challenge
- UI :
  - `AtelierAgentSidebar.tsx` (left rail dans l'onglet Atelier IA) : liste agents avec status badges, bouton « Lancer l'analyse de cet agent »
  - `AtelierIATab.tsx` redesign : composer multi-checkbox (max 3) + thread groupé par round
- Persistence :
  - Analyses agent : stockées dans `tender_agent_analyses` (1 row par agent par tender, unique)
  - Messages chat : `tender_chat_messages.metadata.challenge_round` (0=initial, 1=challenge1, 2=challenge2)
  - Liens entre agents d'un même round via `metadata.turn_id` (uuid groupant les bulles d'un même envoi)

---

## Structure de fichiers

```
supabase/migrations/
└─ 015_agent_analyses.sql              # nouvelle migration

types/db.ts                             # +DbAgentAnalysis, +AnalysisStatus

lib/db/
└─ agent-analyses.ts                   # CRUD per-agent analyses

services/ai/
├─ chat.ts                             # +challenge mode (option dans ChatInput)
└─ prompts/
   ├─ initial-analysis/                # 7 prompts pour analyse initiale
   │  ├─ general.v1.ts
   │  ├─ lecteur-ao.v1.ts
   │  ├─ memoire-technique.v1.ts
   │  ├─ contradicteur.v1.ts
   │  ├─ financier.v1.ts
   │  ├─ terrain.v1.ts
   │  └─ conformite.v1.ts
   └─ chat-challenge/                  # adaptation du prompt chat pour mode challenge
      └─ instructions.v1.ts            # bloc commun « tu vas réagir aux autres agents »

app/(dashboard)/tenders/[id]/
├─ atelier-actions.ts                  # +runAgentInitialAnalysisAction
│                                      # +runChallengeRoundAction
│                                      # ~sendChatMessageAction (accepte agent_names[])
├─ AtelierAgentSidebar.tsx             # nouveau : liste 7 agents + badges + boutons run-analysis
├─ AtelierIATab.tsx                    # redesign : checkbox multi + thread par round
└─ AtelierMessageThread.tsx            # nouveau : rendu groupé par turn/round (sortie de la logique du tab)

tests/services/
└─ challenge.test.ts                   # 1 test mock pour challenge round
```

---

## Pré-requis

- ✅ Plan 3.5 mergé. Tables `tender_chat_messages` + `tender_chat_attachments` dispo.
- ✅ `chatWithAgent`, 7 prompts chat existants
- ✅ `getAIProvider`, `MockProvider`, `withAITracking`
- ✅ Sidebar layout du tender détail (depuis le commit `1956298`)

---

## Task 1 : Migration 015 + types + lib/db/agent-analyses.ts

**Files:**
- Create: `supabase/migrations/015_agent_analyses.sql`
- Modify: `types/db.ts`
- Create: `lib/db/agent-analyses.ts`

- [ ] **Step 1.1 : Migration**

```sql
-- Plan 3.6 — Analyses par agent metier sur un AO
-- Une row par (tender_id, agent_name) : chaque agent peut produire sa propre
-- analyse initiale du document. Status badges sur la sidebar Atelier IA.

create type agent_analysis_status as enum ('pending', 'running', 'ready', 'failed');

create table public.tender_agent_analyses (
  id          uuid primary key default gen_random_uuid(),
  tender_id   uuid not null references tenders(id) on delete cascade,
  agent_name  text not null,
  status      agent_analysis_status not null default 'pending',
  summary     text,                          -- résumé court (3-5 phrases) de la perspective de l'agent
  key_points  jsonb,                         -- bullets : risks, opportunities, key_metrics, etc. (shape libre par agent)
  raw_content text,                          -- markdown long si l'agent en produit
  metadata    jsonb,                         -- provider, model, tokens, durée
  error_msg   text,
  created_at  timestamptz default now(),
  updated_at  timestamptz default now(),
  unique (tender_id, agent_name)
);

create index tender_agent_analyses_tender_idx on public.tender_agent_analyses(tender_id);

alter table public.tender_agent_analyses enable row level security;

create policy "agent analyses manager admin all" on public.tender_agent_analyses
  for all using (public.current_user_role() in ('admin', 'manager'))
  with check (public.current_user_role() in ('admin', 'manager'));
```

- [ ] **Step 1.2 : Apply via `npm run db:push`**

Expected: « Applying 015_agent_analyses.sql ✓ … Total tracked: 15 ».

- [ ] **Step 1.3 : Étendre `types/db.ts`**

```ts
export type AgentAnalysisStatus = 'pending' | 'running' | 'ready' | 'failed'

export interface DbAgentAnalysis {
  id: string
  tender_id: string
  agent_name: ChatAgentName
  status: AgentAnalysisStatus
  summary: string | null
  key_points: Record<string, unknown> | null
  raw_content: string | null
  metadata: Record<string, unknown> | null
  error_msg: string | null
  created_at: string
  updated_at: string
}
```

- [ ] **Step 1.4 : `lib/db/agent-analyses.ts`**

```ts
import { createClient as createServerClient } from '@/lib/supabase/server'
import { createAdminClient } from '@/lib/supabase/admin'
import type { DbAgentAnalysis, ChatAgentName, AgentAnalysisStatus } from '@/types/db'

export async function listAgentAnalyses(tenderId: string): Promise<DbAgentAnalysis[]> {
  const supabase = await createServerClient()
  const { data, error } = await supabase
    .from('tender_agent_analyses')
    .select('id, tender_id, agent_name, status, summary, key_points, raw_content, metadata, error_msg, created_at, updated_at')
    .eq('tender_id', tenderId)
  if (error) throw error
  return (data ?? []) as DbAgentAnalysis[]
}

export async function getAgentAnalysis(tenderId: string, agent: ChatAgentName): Promise<DbAgentAnalysis | null> {
  const supabase = await createServerClient()
  const { data, error } = await supabase
    .from('tender_agent_analyses')
    .select('id, tender_id, agent_name, status, summary, key_points, raw_content, metadata, error_msg, created_at, updated_at')
    .eq('tender_id', tenderId)
    .eq('agent_name', agent)
    .maybeSingle()
  if (error || !data) return null
  return data as DbAgentAnalysis
}

export async function upsertAgentAnalysis(input: {
  tender_id: string
  agent_name: ChatAgentName
  status: AgentAnalysisStatus
  summary?: string | null
  key_points?: Record<string, unknown> | null
  raw_content?: string | null
  metadata?: Record<string, unknown> | null
  error_msg?: string | null
}): Promise<string> {
  const supabase = createAdminClient()
  const { data, error } = await supabase
    .from('tender_agent_analyses')
    .upsert(
      {
        tender_id: input.tender_id,
        agent_name: input.agent_name,
        status: input.status,
        summary: input.summary ?? null,
        key_points: input.key_points ?? null,
        raw_content: input.raw_content ?? null,
        metadata: input.metadata ?? null,
        error_msg: input.error_msg ?? null,
        updated_at: new Date().toISOString(),
      },
      { onConflict: 'tender_id,agent_name' }
    )
    .select('id')
    .single()
  if (error || !data) throw error ?? new Error('No id')
  return data.id
}
```

- [ ] **Step 1.5 : Verify + commit**

```bash
npx tsc --noEmit
git add supabase/migrations/015_agent_analyses.sql types/db.ts lib/db/agent-analyses.ts
git commit -m "feat(atelier-3.6): migration 015 + types + lib/db/agent-analyses"
```

---

## Task 2 : 7 prompts initial-analysis + Server Action runAgentInitialAnalysis

**Files:**
- Create: `services/ai/prompts/initial-analysis/{general,lecteur-ao,memoire-technique,contradicteur,financier,terrain,conformite}.v1.ts`
- Modify: `app/(dashboard)/tenders/[id]/atelier-actions.ts`

- [ ] **Step 2.1 : 7 prompts d'analyse initiale**

Chaque prompt produit une analyse courte structurée :

```
{
  summary: string,           // 3-5 phrases
  key_points: {
    strengths?: string[],
    risks?: string[],
    blockers?: string[],
    opportunities?: string[],
    metrics?: Record<string, string>,  // pour financier/terrain
  },
  raw_content: string,       // markdown long, optionnel
}
```

Pattern type pour `lecteur-ao` :
```ts
export const LECTEUR_AO_INIT_V1 = {
  version: 'v1',
  modelTier: 'heavy' as const,
  system: `Tu es l'agent "lecteur AO". Tu lis le PDF d'AO et tu produis une analyse structurée :
- summary (3-5 phrases) : ton verdict sur la lisibilité, les contraintes principales et les risques.
- key_points :
  * blockers : tout ce qui rendrait la candidature impossible
  * risks : les zones de difficulté à anticiper
  * strengths : les éléments du dossier qui sont en notre faveur
- raw_content : analyse longue en markdown (optionnel, max 1500 mots)

Tu réponds STRICTEMENT en JSON matchant le schéma fourni.`,
}
```

Décliner pour les 7 agents avec un focus métier propre à chaque persona. Les fixtures mock retourneront des `key_points` plausibles selon l'agent (financier → metrics chiffrées, contradicteur → blockers, etc.).

- [ ] **Step 2.2 : Server Action `runAgentInitialAnalysisAction`**

Ajouter à `atelier-actions.ts` :

```ts
const runAnalysisSchema = z.object({
  tender_id: z.string().uuid(),
  agent_name: z.enum(CHAT_AGENTS),
})

export async function runAgentInitialAnalysisAction(formData: FormData) {
  const userId = await requireManagerOrAdmin()
  const parsed = runAnalysisSchema.safeParse({
    tender_id: formData.get('tender_id'),
    agent_name: formData.get('agent_name'),
  })
  if (!parsed.success) return { error: 'Invalid input' }

  // Lock optimiste : passer en 'running'
  await upsertAgentAnalysis({
    tender_id: parsed.data.tender_id,
    agent_name: parsed.data.agent_name,
    status: 'running',
  })

  // Schedule analyse via after()
  after(async () => {
    try {
      const tender = await getTender(parsed.data.tender_id)
      if (!tender) throw new Error('Tender introuvable')
      const doc = await getTenderDocument(parsed.data.tender_id)
      if (!doc?.extracted_text) throw new Error('Pas de texte extrait')

      const lib = await buildLibraryContext()
      const result = await runInitialAnalysisAgent({
        agentName: parsed.data.agent_name,
        rawText: doc.extracted_text,
        libraryContext: lib.markdown,
        userId,
      })
      await upsertAgentAnalysis({
        tender_id: parsed.data.tender_id,
        agent_name: parsed.data.agent_name,
        status: 'ready',
        summary: result.summary,
        key_points: result.keyPoints,
        raw_content: result.rawContent,
        metadata: result.metadata,
      })
    } catch (e) {
      await upsertAgentAnalysis({
        tender_id: parsed.data.tender_id,
        agent_name: parsed.data.agent_name,
        status: 'failed',
        error_msg: e instanceof Error ? e.message : 'unknown',
      })
    }
  })

  revalidatePath(`/tenders/${parsed.data.tender_id}`)
  return { ok: true }
}
```

(Une nouvelle fonction `runInitialAnalysisAgent` est à ajouter dans `services/ai/initial-analysis.ts` — orchestre l'appel d'un seul agent en mode initial-analysis avec son prompt v1, en mode mock retourne un fixture par agent.)

- [ ] **Step 2.3 : Verify + commit**

```bash
npx tsc --noEmit
git add services/ai/prompts/initial-analysis/ services/ai/initial-analysis.ts "app/(dashboard)/tenders/[id]/atelier-actions.ts"
git commit -m "feat(atelier-3.6): 7 prompts initial-analysis + runAgentInitialAnalysisAction"
```

---

## Task 3 : Multi-agent chat avec parallélisme

**Files:**
- Modify: `app/(dashboard)/tenders/[id]/atelier-actions.ts`

- [ ] **Step 3.1 : `sendChatMessageAction` accepte `agent_names: string[]` (1-3)**

```ts
const multiAgentSchema = z.object({
  tender_id: z.string().uuid(),
  agent_names: z.array(z.enum(CHAT_AGENTS)).min(1).max(3),
  message: z.string().min(1).max(4000),
})
```

À l'envoi :
1. Insert user message (1)
2. Pour chaque agent de la liste : `Promise.all(agents.map(agent => chatWithAgent({...}, agent)))`
3. Insert N agent messages avec `metadata.turn_id` commun (uuid généré au début) et `metadata.challenge_round = 0`
4. Retourner `{ ok, userMessage, agentMessages: [...] }` (array)

- [ ] **Step 3.2 : Verify + commit**

```bash
npx tsc --noEmit
git add "app/(dashboard)/tenders/[id]/atelier-actions.ts"
git commit -m "feat(atelier-3.6): sendChatMessageAction accepte 1-3 agents en parallele"
```

---

## Task 4 : Server Action runChallengeRoundAction

**Files:**
- Modify: `app/(dashboard)/tenders/[id]/atelier-actions.ts`
- Modify: `services/ai/chat.ts` (mode challenge)

- [ ] **Step 4.1 : Ajouter mode challenge à `chatWithAgent`**

```ts
export interface ChatInput {
  // ... existant
  challengeContext?: { otherAgents: { agent: ChatAgentName; content: string }[] }
}
```

Si `challengeContext` fourni, le prompt agent reçoit en plus :
```
=== Réponses des autres agents (round précédent) ===
[Contradicteur] : ...
[Financier] : ...

Ta mission : reagir, contester, complementer leurs reponses depuis ta perspective d'agent <X>.
```

- [ ] **Step 4.2 : Server Action `runChallengeRoundAction`**

```ts
const challengeSchema = z.object({
  tender_id: z.string().uuid(),
  turn_id: z.string().uuid(),                     // identifie le turn parent
  current_round: z.number().int().min(0).max(1), // 0 ou 1 (max 2 rounds total)
})

export async function runChallengeRoundAction(formData: FormData) {
  const userId = await requireManagerOrAdmin()
  const parsed = challengeSchema.safeParse({...})
  if (!parsed.success) return { error: 'Invalid' }

  // Récupérer toutes les bulles agent du turn courant + round
  const messages = await listChatMessagesForTurn(parsed.data.tender_id, parsed.data.turn_id)
  const previousRound = messages.filter(m => m.role === 'agent' && m.metadata?.challenge_round === parsed.data.current_round)
  if (previousRound.length < 2) return { error: 'Au moins 2 agents requis pour un challenge' }
  
  // Pour chaque agent du round précédent, lancer en parallèle un challenge avec context des autres
  const nextRound = parsed.data.current_round + 1
  if (nextRound > 2) return { error: 'Max 2 rounds de challenge atteint' }

  await Promise.all(previousRound.map(async (m) => {
    const others = previousRound.filter(o => o.id !== m.id)
    // ... call chatWithAgent avec challengeContext, insert avec metadata.challenge_round=nextRound, metadata.turn_id=parsed.data.turn_id
  }))

  return { ok: true, agentMessages: [...] }
}
```

- [ ] **Step 4.3 : Verify + commit**

---

## Task 5 : UI — AtelierAgentSidebar

**Files:**
- Create: `app/(dashboard)/tenders/[id]/AtelierAgentSidebar.tsx`

- [ ] **Step 5.1 : Composant agent sidebar**

Liste verticale des 7 agents avec :
- Avatar + nom + spécialité
- Status badge :
  - `pending` (gris) « Pas encore analysé »
  - `running` (bleu pulsant) « Analyse en cours »
  - `ready` (vert) « Analyse faite »
  - `failed` (rouge) « Échec »
- Si `key_points.risks` non vide → badge orange « Risque détecté »
- Bouton « Lancer l'analyse » si status = pending ou failed
- Click sur l'agent → ouvre un drawer avec son `summary` + `raw_content` + `key_points`

- [ ] **Step 5.2 : Verify + commit**

---

## Task 6 : UI — AtelierIATab redesign

**Files:**
- Modify: `app/(dashboard)/tenders/[id]/AtelierIATab.tsx`
- Create: `app/(dashboard)/tenders/[id]/AtelierMessageThread.tsx`

- [ ] **Step 6.1 : Composer multi-checkbox**

Remplacer le single `<select>` par :
- Liste de checkboxes (7 agents) avec compteur « 1/3 sélectionnés »
- Disabled si > 3 agents cochés
- Si 0 agent → disable Send button

- [ ] **Step 6.2 : Thread groupé par turn/round**

Composant `AtelierMessageThread` :
- Group messages by `metadata.turn_id`
- Pour chaque turn :
  - User bubble (à droite)
  - Round 0 : N bulles agent (parallèles, alignées en grid)
  - Si Round 1 existe : séparateur « Challenge round 1 » + N bulles agent
  - Si Round 2 existe : idem
- Bouton « Lancer un challenge » sous le dernier round (visible seulement si <2 rounds et ≥2 agents)

- [ ] **Step 6.3 : Verify + commit**

---

## Task 7 : Tests + final review

**Files:**
- Create: `tests/services/challenge.test.ts`

- [ ] **Step 7.1 : Test mock challenge**

Vérifier que `chatWithAgent` en mode challenge inclut bien le contexte des autres agents dans le prompt et retourne du content.

- [ ] **Step 7.2 : Verify + final review**

---

## Critères d'acceptance Plan 3.6

- [ ] Migration 015 appliquée, table `tender_agent_analyses` créée + RLS
- [ ] Sidebar atelier IA affiche 7 agents avec statut + bouton « Lancer l'analyse »
- [ ] Lancer l'analyse d'un agent → status passe à `running` puis `ready` (mock instant)
- [ ] Composer multi-checkbox max 3 agents, compteur visible, Send désactivé si 0
- [ ] Envoyer un message avec 2-3 agents → 2-3 bulles parallèles dans le thread
- [ ] Bouton « Lancer un challenge » apparaît sous un turn avec ≥2 agents et <2 rounds déjà faits
- [ ] Cliquer challenge → 2-3 bulles supplémentaires (round suivant) où chaque agent voit les réponses des autres
- [ ] Max 2 rounds de challenge → bouton disparaît après round 2
- [ ] Multi-tenant readiness : 0 `.from('tender_agent_analyses')` outside lib/db
- [ ] tsc passe, tous tests passent
- [ ] Coût IA tracé dans `ai_usage` pour chaque appel agent (initial-analysis + chat + challenge)

## Hors-scope explicite

- Streaming token-par-token (Plan 3.7+)
- Mémoire long terme cross-tender
- Plus de 3 agents simultanés (cap dur)
- Plus de 2 rounds de challenge (cap dur)
- Édition / suppression de messages dans le thread
- Persona personnalisée par agent (juste les 7 prédéfinis)
- Agent `stratege_commercial` (Plan 3.7+)
